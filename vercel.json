{
    "builds": [{
        "src": "ticketswap/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "ticketswap/wsgi.py"
        }
    ]
}